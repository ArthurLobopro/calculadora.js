{
    "files.exclude": {
        "**/.git": true,
        "node_modules": true,
        "**/*.css": {
            "when": "$(basename).scss"
        },
        "**/*.js": {
            "when": "$(basename).tsx"
        }
    },
    "svg.preview.background": "black"
}